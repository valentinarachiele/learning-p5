var canvas;

function setup() {
	canvas = createCanvas(500, 500);
	background(90, 120, 30);
	canvas.parent('prescelto'); //può essere scritto anche senza #, però nell'HTML dobbiamo usare un ID non una CLASSE
}

function draw() {

}
